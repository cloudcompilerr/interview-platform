{
  "name": "interview-platform",
  "description": "Zero-cost, offline-first interview management and candidate assessment platform.",
  "version": "1.0.0",
  "private": true,
  "homepage": ".",
  "license": "MIT",
  "keywords": ["interview", "recruitment", "offline", "zero-cost"],
  "repository": {
    "type": "git",
    "url": "https://github.com/cloudcompilerr/interview-platform.git"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build",
    "export-data": "node scripts/export-data.js"
  },
  "eslintConfig": {
    "extends": ["react-app"]
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
